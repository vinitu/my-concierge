# Runtime Config

Store mutable runtime config files in this directory.

Current files:

- `worker.json`: `assistant-worker` runtime settings

Current rule:

- `worker.json` is created and updated locally by the running worker
- runtime config files are not tracked in git
